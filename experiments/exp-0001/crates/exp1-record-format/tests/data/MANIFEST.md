# Slice A independent-oracle fixture manifest

These literals are transcribed, not generated by the implementation under test.

| File | Authority | Literal meaning |
|---|---|---|
| `r5-v1.hex` | `docs/experiments/EXP-0001/R5-PHYSICAL-RECORD-INTEGRITY-AND-RECOVERABLE-COMMIT-CONTRACT.md`, §6 V1 | Minimum structural binding |
| `r5-v2.hex` | same, §6 V2 | Provisional record with `DATA` core |
| `r5-v3.hex` | same, §6 V3 | CRC-32C reservation, stored CRC `41f0e427` |
| `r5-v4.hex` | same, §6 V4 | CRC-32C final event, stored CRC `8a1267a2` |
| `crc32c-check.txt` | same, §4 | ASCII `123456789` → `e3069283` |

R7's `R7-PHYSICAL-RECORD-EXAMPLES.md` contains `EXP1-R7-JSON-JCS-1` benchmark-record examples, not `EXP1-B1-RF1` bytes. It therefore supplies no applicable Slice A golden record. This discrepancy is preserved rather than silently reinterpreting those JSON vectors.
